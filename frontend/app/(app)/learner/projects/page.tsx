import { LearnerProjects } from "@/components/modules/scratch-projects";

export const metadata = { title: "Projects" };

/** Unified learner project shelf for saved Scratch and coding work. */
export default function LearnerProjectsPage() {
  return <LearnerProjects />;
}
