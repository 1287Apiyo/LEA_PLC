import { ReportsCenter } from "@/components/modules/reports-center";

export const metadata = { title: "Reports" };

/** Reports center — exportable analytics across every domain. */
export default function ReportsPage() {
  return <ReportsCenter />;
}
