import { LearnerPortfolio } from "@/components/modules/learner-portfolio";

export const metadata = { title: "Portfolio" };

/** Learner portfolio — projects, screenshots and reflections. */
export default function PortfolioPage() {
  return <LearnerPortfolio />;
}
