import { AnalyticsOverview } from "@/components/modules/analytics-overview";

export const metadata = { title: "Learner Analytics" };

/** Learner analytics — engagement, attendance and performance charts. */
export default function AnalyticsPage() {
  return <AnalyticsOverview />;
}
